/// Defines runtime configuration for the Logger SDK.
///
/// Populate this file with the configuration fields shared across components
/// such as batching thresholds, persistence tuning, and upload constraints.
class LoggerConfig {
  const LoggerConfig();
}
